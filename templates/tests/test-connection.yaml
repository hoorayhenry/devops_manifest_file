apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "techdemand.fullname" . }}-test-connection"
  labels:
    {{- include "techdemand.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "techdemand.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
