apiVersion: v1
kind: Service
metadata:
  name: {{ include "techdemand.fullname" . }}
  labels:
    {{- include "techdemand.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "techdemand.selectorLabels" . | nindent 4 }}
